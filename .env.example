# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection (if using MongoDB)
MONGODB_URI=mongodb://localhost:27017/gita_admin

# JWT Secret (change this to a random secure string)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Admin Credentials (default admin - change after first login)
DEFAULT_ADMIN_EMAIL=admin@gitagita.com
DEFAULT_ADMIN_PASSWORD=Admin@123456

# CORS Origin (your admin panel URL in production)
CORS_ORIGIN=http://localhost:3000

# API Key for mobile app communication
MOBILE_API_KEY=gita_mobile_app_key_2024
