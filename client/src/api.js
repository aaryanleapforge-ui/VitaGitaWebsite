// `api.js` removed: admin panel is now Firebase-only and uses the
// Firebase JS SDK (auth, firestore, storage) directly. Any server
// REST calls were removed as part of the migration.

const api = null;
export default api;
